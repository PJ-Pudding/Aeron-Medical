# 🛡️ THE 8-STEP LOOP & PROJECT PROTOCOL (AERON MEDICAL)

> [!IMPORTANT]
> เอกสารฉบับนี้คือกฎเหล็กและวิธีคิดเชิงปฏิบัติการของ AI ในโปรเจกต์ AERON MEDICAL
> เพื่อป้องกันการเดา (Hallucination), ตัดการมโน, และสร้างผลลัพธ์ที่พิสูจน์ได้จริง 100%

---

## 🔁 THE 8-STEP OPERATIONAL LOOP

### 1. 📖 Read Fully First (อ่านให้ทะลุก่อน ห้ามเดา)
* ห้ามอ่านเฉพาะจุดแล้วเดาภาพรวม ต้องอ่านโค้ดที่เกี่ยวข้องให้ครบทั้ง Loop (UI ➔ Hook ➔ Storage ➔ Cloud) ให้เข้าใจ End-to-End Flow ก่อนเสมอ

### 2. 🔲 4-Box Framing (ตีกรอบขอบเขตให้ชัด)
* กำหนดขอบเขต 4 มิติทุกครั้ง:
  * **Goal**: ปัญหาที่แท้จริงคืออะไร สิ่งที่ต้องทำให้สำเร็จคืออะไร
  * **In-Scope**: ไฟล์และฟังก์ชันที่จำเป็นต้องแตะต้อง
  * **Out-of-Scope**: โมดูลข้างเคียงที่ห้ามดัดแปลงเด็ดขาด
  * **Constraints**: ข้อจำกัด เช่น ห้ามล้างแคชทิ้ง, ห้ามใช้ Plaintext Token

### 3. 🎯 Unknown First & Reality Check (พุ่งชนจุดเสี่ยง & พิสูจน์ด้วย Terminal)
* เจาะจุดที่เสี่ยงที่สุดหรือไม่รู้ที่สุดก่อนเสมอ (De-risk First) เช่น ทดสอบการเชื่อมต่อ API, CORS, หรือ Encoding
* "ห้ามมโน ห้ามสันนิษฐาน" ทุกข้อสงสัยต้องพิสูจน์ด้วยการรันคำสั่งจริงใน Terminal ผ่านสคริปต์ทดสอบ และต้องได้ผลลัพธ์ Exit Code 0 เท่านั้น

### 4. 🪞 Self-Refutation (ตั้งคำถามหักล้างตัวเอง)
* ตั้งคำถามเพื่อจับผิดสมมติฐานตัวเองล่วงหน้าเสมอ:
  * *"มีกรณีไหนบ้างที่แนวคิดนี้จะผิดหรือพัง?"*
  * *"ถ้าผู้ใช้เปิดสองหน้าจอพร้อมกัน จะเกิด Race Condition หรือไม่?"*
  * *"ถ้าข้อมูลส่งมาเป็นค่าว่าง [] จะมีตัวไหนแอบดึงค่าตั้งต้นมาชุบชีวิตหรือไม่?"*

### 5. 📋 Plan Proposal & Mandatory Confirmation (นำเสนอแผน & ขอคอนเฟิร์มก่อนทำ) ⚠️ [กฎเหล็ก]
* **ก่อนแตะต้องหรือแก้ไขซอร์สโค้ดจริงเด็ดขาด**:
  1. ต้องสรุปแผนการทำงานและจุดที่จะแก้ไขเป็น **Bullet Points** ที่กระชับ ชัดเจน เข้าใจง่าย
  2. **ต้อง "หยุด (STOP)" และรอคำอนุมัติยืนยันจากผู้ใช้ก่อนเสมอ** (เช่น คำว่า *"ตกลง"*, *"ทำเลย"*, *"จัดไป"*) 
  3. ห้ามคิดเองเออเองแล้วลงมือแก้โค้ดไปก่อนโดยที่ผู้ใช้ยังไม่อนุญาตเด็ดขาด!

### 6. ⚡ Anti-Stall Execution (เมื่อได้รับอนุมัติ ให้ลุยทันทีไม่ถ่วงเวลา)
* มี Bias for Action เสมอ:
  * ในขั้นตอนสืบค้น/ทดสอบ (ข้อ 1-4): ให้สั่งเครื่องมืออ่านไฟล์และทดสอบทันที ห้ามถามคำถามลอยๆ ในเรื่องที่ตรวจสอบเองได้
  * เมื่อได้รับคำอนุมัติจากข้อ 5 แล้ว: ให้สั่งรันเครื่องมือลงมือแก้ไขโค้ดทันที ตัดบทสนทนาเยิ่นเย้อที่ไร้ประโยชน์

### 7. 🌐 Blast Radius Guard (คุมแรงกระเพื่อม Zero Side-Effects)
* การแก้ไขปัญหาจุดหนึ่ง ต้องไม่สร้างปัญหาใหม่ในอีกจุดหนึ่ง ตรวจสอบผลกระทบต่อ 22 ตาราง และสิทธิ์ของทุก Role (Owner, Admin, Sales, Messenger) เสมอ

### 8. 🔒 Closed-Loop Verification & Backup (ปิดลูปสมบูรณ์และมีจุดถอย)
* ทุกงานต้องจบแบบปิดลูปสมบูรณ์ตามลำดับนี้เสมอ:
  1. **Pre-Backup**: รัน `node backup.js` ก่อนเริ่มแตะโค้ด
  2. **Verify**: รัน `node build.js` (Babel Pre-compile) และ `node verify_system.js` ต้องผ่าน 100%
  3. **Security**: ตรวจสอบว่าไม่มี Plaintext Secret Token ในโค้ด
  4. **Post-Backup**: รัน `node backup.js` หลังทำเสร็จ พร้อมระบุเวอร์ชัน `index.html (?v=...)`

---

## 📌 GENERAL COMMUNICATION RULES
1. **Strict Thai Language**: ตอบสนองและรายงานผู้ใช้ด้วยภาษาไทยที่สุภาพ ชัดเจน กระชับ และตรงประเด็นเสมอ
2. **Zero Plaintext Secrets**: ห้ามฮาร์ดโค้ดหรือแสดง Token ความลับของระบบเด็ดขาด
