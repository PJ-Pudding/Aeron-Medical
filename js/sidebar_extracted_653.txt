  // Pop-up Sidebar Drawer State & 8 Primary Navigation Tabs
  const [isSidebarOpen, setIsSidebarOpen] = useState(false);
  const [activeSidebarTab, setActiveSidebarTab] = useState('dashboard'); // 'dashboard' | 'clients' | 'project' | 'logistic' | 'calendar' | 'report' | 'finance' | 'hr'
  const [logisticSubView, setLogisticSubView] = useState('product_catalog'); // 'product_catalog' | 'shipment_tracking' | 'repair_service' | 'sold_products'
  const [reportSubView, setReportSubView] = useState('fda_registration'); // 'fda_registration' | 'penalty_report' | 'activity_logs'
  const [financeSubView, setFinanceSubView] = useState('cost_calculation'); // 'cost_calculation' | 'purchase_orders'
  const [hrSubView, setHRSubView] = useState('leave_attendance'); // 'leave_attendance' | 'team_roster'

  return (
    <div className="min-h-screen bg-slate-950 text-slate-100 font-sans selection:bg-emerald-500 selection:text-slate-950">
      
      {/* Pop-up Sidebar Navigation Modal (8 Primary Tabs) */}
      <SidebarNavDrawer
        isOpen={isSidebarOpen}
        onClose={() => setIsSidebarOpen(false)}
        activeTab={activeSidebarTab}
        setActiveTab={setActiveSidebarTab}
        currentUser={currentUser}
        pendingPOCount={pendingPOCount}
        activeRepairCount={activeRepairCount}
        activeShipmentCount={activeShipmentCount}
        activeFDACount={activeFDACount}
      />

      {/* Main Full-Width Content Container */}
      <div className="min-w-0 flex flex-col min-h-screen">
        
        {/* Top Navigation Header & Action Bar */}
        <Header 
          onToggleSidebar={() => setIsSidebarOpen(true)}
          activeSidebarTab={activeSidebarTab}
          setActiveSidebarTab={setActiveSidebarTab}
          activeView={activeView}
          setActiveView={setActiveView}