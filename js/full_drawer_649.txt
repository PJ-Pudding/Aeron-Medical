// ----------------------------------------------------
// Sidebar Pop-up Navigation Modal Component (8 Primary Tabs)
// ----------------------------------------------------
function SidebarNavDrawer({ isOpen, onClose, activeTab, setActiveTab, currentUser, pendingPOCount, activeRepairCount, activeShipmentCount, activeFDACount }) {
  if (!isOpen) return null;

  const menuItems = [
    { id: 'dashboard', label: 'Dashboard', icon: '📊', badge: null, desc: 'ภาพรวมผลงาน & ดัชนีตัวเลขสำคัญ' },
    { id: 'clients', label: 'Clients', icon: '🏥', badge: null, desc: 'ฐานข้อมูลลูกค้า รพ. & แพทย์ผู้สั่งซื้อ' },
    { id: 'project', label: 'Project', icon: '📋', badge: null, desc: 'กระดาน Sales Kanban & โครงการ' },
    { id: 'logistic', label: 'Logistic', icon: '🚚', badge: (activeShipmentCount + activeRepairCount) > 0 ? (activeShipmentCount + activeRepairCount) : null, desc: 'คลังสินค้า, ขนส่ง & ส่งซ่อม' },
    { id: 'calendar', label: 'Calendar', icon: '📅', badge: null, desc: 'ปฏิทินจอง Demo & เซ็นรับของ' },
    { id: 'report', label: 'Report', icon: '📑', badge: activeFDACount > 0 ? activeFDACount : null, desc: 'ทะเบียน อย. & ประวัติการใช้งาน' },
    { id: 'finance', label: 'Finance', icon: '💰', badge: pendingPOCount > 0 ? pendingPOCount : null, desc: 'คำนวณต้นทุน & ใบสั่งซื้อ PO' },
    { id: 'hr', label: 'HR', icon: '👥', badge: null, desc: 'ตารางวันลา & ขาด ลา มาสาย' }
  ];

  return (
    <div className="fixed inset-0 z-50 bg-slate-950/80 backdrop-blur-md flex">
      {/* Backdrop overlay listener */}
      <div className="absolute inset-0" onClick={onClose} />

      {/* Slide-out Pop-up Drawer */}
      <aside className="relative z-10 bg-slate-900 border-r border-slate-800 w-80 max-w-[85vw] h-full shadow-2xl flex flex-col justify-between p-5 space-y-4 animate-modal overflow-y-auto">
        <div className="space-y-5">
          
          {/* Drawer Header */}
          <div className="flex items-center justify-between pb-4 border-b border-slate-800">
            <div className="flex items-center gap-3">
              <div className="bg-white p-1 rounded-xl shadow-lg border border-slate-700 flex items-center justify-center h-10 w-10 flex-shrink-0 overflow-hidden">
                <img src="./assets/logo.jpg" alt="AERON Logo" className="h-full w-full object-contain" />
              </div>
              <div>
                <h1 className="text-sm font-black tracking-wider leading-tight flex items-center gap-1">
                  <span className="bg-gradient-to-r from-lime-400 via-emerald-400 to-green-500 bg-clip-text text-transparent font-extrabold">AERON</span>
                  <span className="text-white font-bold">MEDICAL</span>
                </h1>
                <p className="text-[10px] text-slate-400">เมนูระบบหลัก (8 แท็บ)</p>
              </div>
            </div>
            
            <button
              onClick={onClose}
              className="p-2 bg-slate-800 hover:bg-slate-700 text-slate-400 hover:text-white rounded-xl text-sm font-bold transition-colors"
              title="ปิดเมนู"
            >
              ✕
            </button>
          </div>

          {/* 8 Primary Menu Tabs */}
          <nav className="space-y-2">
            {menuItems.map(item => {
              const isActive = activeTab === item.id;
              return (
                <button
                  key={item.id}
                  onClick={() => {
                    setActiveTab(item.id);
                    onClose();
                  }}
                  className={`w-full flex items-center justify-between p-3.5 rounded-2xl transition-all ${
                    isActive
                      ? 'bg-gradient-to-r from-emerald-600 to-teal-600 text-white font-extrabold shadow-lg shadow-emerald-600/30'
                      : 'text-slate-300 hover:text-white hover:bg-slate-800/80 border border-transparent hover:border-slate-700/80'
                  }`}
                >
                  <div className="flex items-center gap-3.5">
                    <span className="text-2xl flex-shrink-0">{item.icon}</span>
                    <div className="text-left">
                      <div className="text-xs font-bold leading-none">{item.label}</div>
                      <div className={`text-[10.5px] mt-1 font-normal ${isActive ? 'text-emerald-100' : 'text-slate-400'}`}>{item.desc}</div>
                    </div>
                  </div>

                  {item.badge !== null && (
                    <span className={`px-2.5 py-0.5 rounded-full text-[10px] font-mono font-bold ${
                      isActive ? 'bg-white text-emerald-900' : 'bg-rose-500/20 text-rose-300 border border-rose-500/30'
                    }`}>
                      {item.badge}
                    </span>
                  )}
                </button>
              );
            })}
          </nav>
        </div>

        {/* Drawer Footer */}
        <div className="pt-4 border-t border-slate-800 space-y-2 text-xs">
          {currentUser && (
            <div className="flex items-center justify-between bg-slate-950 p-3 rounded-xl border border-slate-800">
              <div className="flex items-center gap-2">
                <span className="text-lg">{currentUser.avatar}</span>
                <div>
                  <div className="font-bold text-white text-xs">{currentUser.fullName}</div>
                  <div className="text-[10px] text-amber-300 font-semibold">{currentUser.roleLabel}</div>
                </div>
              </div>
            </div>
          )}
          <button
            onClick={onClose}
            className="w-full py-2.5 bg-slate-800 hover:bg-slate-700 text-slate-300 hover:text-white font-bold rounded-xl text-xs transition-colors"
          >
            ✕ ปิดเมนูหลัก
          </button>
        </div>
      </aside>
    </div>
  );
}

// ----------------------------------------------------
// Header Component (Top Action Bar)
// ----------------------------------------------------
function Header({ onToggleSidebar, activeSidebarTab, setActiveSidebarTab, activeView, setActiveView, logisticSubView, setLogisticSubView, reportSubView, setReportSubView, financeSubView, setFinanceSubView, hrSubView, setHRSubView, members, projects = [], pendingPOCount, activeRepairCount, soldProductsCount, activeShipmentCount, activeFDACount, onOpenNewModal, onOpenMemberModal, searchTerm, setSearchTerm, filterClientType, setFilterClientType, filterBudgetType, setFilterBudgetType, exportToCSV, onResetDemo, onOpenDemoModal, onOpenProductModal, onOpenRepairModal, onOpenSoldModal, onOpenShipmentModal, onOpenFDAModal, onOpenAiModal, currentUser, onLogout, onOpenActivityLogs }) {
  
  // Compute current selected value for Top Dropdown
  const currentTopValue = useMemo(() => {
    if (activeSidebarTab === 'dashboard') return 'manager';
    if (activeSidebarTab === 'clients') return 'all_clients';
    if (activeSidebarTab === 'project') return activeView || 'kanban_all';
    if (activeSidebarTab === 'logistic') return logisticSubView || 'product_catalog';
    if (activeSidebarTab === 'calendar') return 'demo_calendar';
    if (activeSidebarTab === 'report') return reportSubView || 'fda_registration';
    if (activeSidebarTab === 'finance') return financeSubView || 'cost_calculation';
    if (activeSidebarTab === 'hr') return hrSubView || 'leave_attendance';
    return 'manager';
  }, [activeSidebarTab, activeView, logisticSubView, reportSubView, financeSubView, hrSubView]);

  const handleTopSelectChange = (val) => {
    if (activeSidebarTab === 'project') {
      setActiveView(val);
    } else if (activeSidebarTab === 'logistic') {
      setLogisticSubView(val);
    } else if (activeSidebarTab === 'report') {
      setReportSubView(val);
    } else if (activeSidebarTab === 'finance') {
      setFinanceSubView(val);
    } else if (activeSidebarTab === 'hr') {
      setHRSubView(val);
    }
  };

  return (
    <header className="sticky top-0 z-30 glass-panel border-b border-slate-800">
      <div className="px-4 py-3 sm:px-6">
        <div className="max-w-7xl mx-auto flex flex-col md:flex-row md:items-center justify-between gap-4">
          
          {/* Menu Pop-up Trigger Button & Title */}
          <div className="flex items-center justify-between sm:justify-start gap-3">
            <button
              onClick={onToggleSidebar}
              className="px-3.5 py-2 bg-gradient-to-r from-emerald-600 via-teal-600 to-emerald-700 hover:from-emerald-500 hover:to-teal-500 text-white font-extrabold rounded-xl text-xs shadow-lg shadow-emerald-600/30 flex items-center gap-2 transition-all hover:scale-105 active:scale-95"
              title="เปิดเมนูหลัก 8 หมวดหมู่ (Pop-up Menu)"
            >
              <span className="text-base font-black">☰</span>
              <span>เมนูหลัก</span>
              <span className="text-[10px] px-1.5 py-0.5 bg-white/20 rounded-md font-mono hidden sm:inline">8 แท็บ</span>
            </button>

            <div className="flex items-center gap-3">
              <div className="bg-white p-1 rounded-xl shadow-lg border border-slate-700 flex items-center justify-center h-10 w-10 sm:h-12 sm:w-12 flex-shrink-0 overflow-hidden">
                <img 
                  src="./assets/logo.jpg" 
                  alt="AERON MEDICAL Logo" 
                  className="h-full w-full object-contain"
                />
              </div>
              <div className="space-y-0.5">
                <h1 className="text-sm sm:text-base font-black tracking-wider leading-tight font-sans flex items-center gap-1.5">
                  <span className="bg-gradient-to-r from-[#a3e635] via-[#65a30d] to-[#16a34a] bg-clip-text text-transparent font-extrabold drop-shadow">
                    AERON
                  </span>
                  <span className="text-white font-bold">
                    MEDICAL
                  </span>
                </h1>
                <div className="text-[11px] font-semibold text-indigo-200/90 tracking-wide flex items-center gap-1.5">
                  <span>Project Tracker</span>
                  <span className="text-[9.5px] px-2 py-0.5 rounded-md bg-emerald-950 text-emerald-300 border border-emerald-800/80 font-bold uppercase">
                    {activeSidebarTab}
                  </span>
                </div>
              </div>
            </div>
          </div>